// let age;

// console.log(age, age + 3, `the age is ${age}`); // undefined NaN "the age is undefined"

let age = null;

console.log(age, age + 3, `the age is ${age}`); // null 3 "the age is null"  so null + 3 is 3, interesting!!


